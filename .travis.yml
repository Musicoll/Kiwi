language: cpp
sudo: true

# No Email notification
notifications:
  email: false

matrix:
  include:
    - os: linux
      compiler: gcc
      addons:
        apt:
          sources: ['ubuntu-toolchain-r-test']
          packages: ['g++-4.9', 'python-pip', 'python-yaml']
      before_install:
        - sudo apt-get update -qq
        - sudo apt-get install cmake
      before_script:
        - pip install --user git+git://github.com/eddyxu/cpp-coveralls.git
      after_success:
        #- make CXXFLAGS="-fprofile-arcs -ftest-coverage -std=c++11 -lstdc++" CXX=$COMPILER
        #- coveralls --exclude test/catch.hpp --include Modules/* --gcov-options '\-lp' --gcov 'gcov-4.9'
      env: COMPILER=g++-4.9

    - os: linux
      compiler: clang
      addons:
        apt:
          sources: ['ubuntu-toolchain-r-test', 'llvm-toolchain-precise-3.6']
          packages: ['clang-3.6']
      before_install:
        - sudo apt-get update -qq
        - sudo apt-get install cmake
      env: COMPILER=clang++-3.6

    - os: osx
      compiler: gcc
      before_install:
        - brew update
        - brew install cmake
      env: COMPILER=g++-4.9

    - os: osx
      compiler: clang++
      before_install:
        - brew update
        - brew install cmake
      env: COMPILER=clang++

script:
  - mkdir build
  - cd build
  - export CXX=$COMPILER
  - cmake ..
  - make

after_success:
  - ./test_core
  - ./test_dsp
